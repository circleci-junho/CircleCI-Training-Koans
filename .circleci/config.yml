version: 2.1

executors:
  linux:
    docker:
      - image: cimg/node:17.2.0
  macos:
    macos:
      xcode: 12.5.1
  # windows:
  #   machine:
  #     image: 'windows-server-2022-gui:current'   
  machine:
    machine: true

orbs:
  win: circleci/windows@2.2.0

jobs:
  build:
    parameters:
      os:
        type: executor
    executor: << parameters.os >>
    steps:
      - run: echo "using << parameters.os >>"

workflows:
  build-workflow:
    jobs:
      - build:
          matrix:
            parameters:
              os: ["linux", "macos", "win/server-2022", "machine"]